# For syntax, see https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax
# This workflow is automatically triggered for every push and pull request, to verify that everything still builds.

name: Continuous Integration
on:
  workflow_call:
  push:
  pull_request:
  merge_group:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # Test a few selected combinations of parameters ("test case matrix").
        # While it would be nice to test all needed combinations, in practice this would a waste of CI usage time.
        # By building just with a few combinations, we only need a few runners,
        # while still achieving adequate coverage.
        # To make a full build with all combinations, use the make_build.yml workflow instead.
        include:
          - target: { platform: linux, arch: x86_64, os: ubuntu-22.04 }
            target-type: template_debug
            float-precision: single
          - target: { platform: windows, arch: x86_64, os: windows-latest }
            target-type: template_release
            float-precision: single
          - target: { platform: macos, arch: universal, os: macos-latest }
            target-type: template_debug
            float-precision: single
          - target: { platform: android, arch: arm64, os: ubuntu-22.04 }
            target-type: template_debug
            float-precision: single
          - target: { platform: web, arch: wasm32, os: ubuntu-22.04 }
            target-type: template_release
            float-precision: single

    runs-on: ${{ matrix.target.os }}
    steps:
      # Clone this repository
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      # Lint
      #- name: Setup clang-format
      #  shell: bash
      #  run: |
      #    python -m pip install clang-format
      #- name: Run clang-format
      #  shell: bash
      #  run: |
      #    clang-format src/** --dry-run --Werror

      # Add linux x86_32 toolchain
      - name: Install multilib support
        if: ${{ matrix.target.platform == 'linux' && matrix.target.arch == 'x86_32' }}
        run: |
          sudo apt-get update && sudo apt-get install -y gcc-multilib g++-multilib

      # Setup dependencies
      - name: Setup godot-cpp
        uses: ./godot-cpp/.github/actions/setup-godot-cpp
        with:
          platform: ${{ matrix.target.platform }}
          em-version: 3.1.62

      # Build GDExtension (with caches)

      - name: Restore .scons_cache
        uses: ./godot-cpp/.github/actions/godot-cache-restore
        with:
          scons-cache: ${{ github.workspace }}/.scons-cache/
          cache-name: ${{ matrix.target.platform }}_${{ matrix.target.arch }}_${{ matrix.float-precision }}_${{ matrix.target-type }}

      - name: Build GDExtension Debug Build
        shell: sh
        env:
          SCONS_CACHE: ${{ github.workspace }}/.scons-cache/
        run: |
          scons target=${{ matrix.target-type }} platform=${{ matrix.target.platform }} arch=${{ matrix.target.arch }} precision=${{ matrix.float-precision }}

      - name: Save .scons_cache
        uses: ./godot-cpp/.github/actions/godot-cache-save
        with:
          scons-cache: ${{ github.workspace }}/.scons-cache/
          cache-name: ${{ matrix.target.platform }}_${{ matrix.target.arch }}_${{ matrix.float-precision }}_${{ matrix.target-type }}

      # --- Reproducer: godot --headless --import SEGVs inside the GDExtension on Godot 4.5 ---
      - name: Install Godot 4.6.3-stable (latest)
        if: ${{ matrix.target.platform == 'linux' && matrix.target.arch == 'x86_64' }}
        run: |
          set -euo pipefail
          ver=4.6.3-stable
          base="https://github.com/godotengine/godot/releases/download/${ver}"
          curl -fsSL "${base}/Godot_v${ver}_linux.x86_64.zip" -o godot.zip
          unzip -q godot.zip
          mv "Godot_v${ver}_linux.x86_64" "$HOME/godot"
          chmod +x "$HOME/godot"
          echo "$HOME" >> "$GITHUB_PATH"
          "$HOME/godot" --version

      - name: godot --headless --import (expected to SEGV)
        if: ${{ matrix.target.platform == 'linux' && matrix.target.arch == 'x86_64' }}
        run: |
          set -euo pipefail
          cd project
          headless_log="$RUNNER_TEMP/headless.log"
          godot --headless --import 2>&1 | tee "$headless_log" || true
          godot --headless --import 2>&1 | tee -a "$headless_log"
          if grep -qF 'handle_crash: Program crashed with signal' "$headless_log"; then
            echo "::error::Confirmed: godot --headless --import SEGV'd inside the GDExtension."
            grep -nE 'handle_crash|signal 11|example_library_init|libgdexample|stack smashing' "$headless_log" | head -20
            exit 1
          fi
          echo "No crash observed."

      - name: xvfb-run godot --import (control — should succeed)
        if: ${{ always() && matrix.target.platform == 'linux' && matrix.target.arch == 'x86_64' }}
        run: |
          set -euo pipefail
          cd project
          xvfb_log="$RUNNER_TEMP/xvfb.log"
          xvfb-run -a godot --import 2>&1 | tee "$xvfb_log" || true
          xvfb-run -a godot --import 2>&1 | tee -a "$xvfb_log"
          if grep -qF 'handle_crash: Program crashed with signal' "$xvfb_log"; then
            echo "::warning::xvfb-run also crashed — deviation from godot#111645 workaround."
          else
            echo "xvfb-run completed without crash."
          fi
